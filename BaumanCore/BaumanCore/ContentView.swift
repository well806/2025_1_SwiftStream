import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            ProfileView()
            
// надо поменять ProfileView на главный экран, когда он будет готов
// а затем прописать логику переходов между экранами
            
        }
    }
}

#Preview {
    ContentView()
}
